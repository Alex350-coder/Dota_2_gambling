/**
 * Placeholder for the app shell commit (T-702) — replaced with the real home page
 * content (live/upcoming markets) in the T-704 commit.
 */
export default function HomePage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-[var(--text-primary)]">P2P Arena</h1>
      <p className="mt-2 text-[var(--text-secondary)]">Simulated-money esports betting.</p>
    </div>
  );
}
